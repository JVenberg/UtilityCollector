{
    "name": "utility-billing-functions",
    "version": "1.0.0",
    "description": "Firebase Cloud Functions for utility billing",
    "main": "lib/index.js",
    "scripts": {
        "build": "tsc",
        "serve": "npm run build && firebase emulators:start --only functions",
        "shell": "npm run build && firebase functions:shell",
        "start": "npm run shell",
        "deploy": "firebase deploy --only functions",
        "logs": "firebase functions:log",
        "validate-bills": "tsx scripts/validate-bills.ts",
        "cleanup-has-adjustments": "tsx scripts/cleanup-has-adjustments.ts"
    },
    "engines": {
        "node": "20"
    },
    "dependencies": {
        "firebase-admin": "^12.0.0",
        "firebase-functions": "^5.0.0",
        "googleapis": "^140.0.0",
        "nodemailer": "^6.9.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "@types/nodemailer": "^6.4.0",
        "tsx": "^4.21.0",
        "typescript": "^5.4.0"
    },
    "private": true
}
